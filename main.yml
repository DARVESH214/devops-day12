name: CI Pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run commands
        run: |
          echo "starting build..."
          ls
          cat index.html

      - name: Build Success
        run: echo "Build Success 🔥"
