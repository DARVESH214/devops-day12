name: CI Pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Show file
        run: cat index.html

      - name: Success
        run: echo "Build Success 🚀"
